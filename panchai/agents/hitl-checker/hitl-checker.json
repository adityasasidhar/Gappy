{
  "name": "hitl-checker",
  "description": "Applies PANCHAI's five-condition HITL escalation matrix and writes hitl_queue entries when human review is required.",
  "instruction": { "$file": "instruction.md" },
  "toolsets": ["POD"],
  "visibility": "POD",
  "output_schema": {
    "type": "object",
    "properties": {
      "session_id": { "type": "string" },
      "hitl_required": { "type": "boolean" },
      "hitl_reason": { "type": "string" },
      "hitl_queue_id": { "type": "string" }
    },
    "required": ["session_id", "hitl_required", "hitl_reason"]
  },
  "permissions": {
    "grants": [
      { "resource_type": "datastore_table", "resource_name": "hitl_queue", "permission_ids": ["datastore.table.read", "datastore.record.read", "datastore.record.write"] },
      { "resource_type": "datastore_table", "resource_name": "debate_sessions", "permission_ids": ["datastore.table.read", "datastore.record.read", "datastore.record.write"] }
    ]
  }
}
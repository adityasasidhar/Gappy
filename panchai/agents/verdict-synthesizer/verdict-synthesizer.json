{
  "name": "verdict-synthesizer",
  "description": "Counts council votes, calculates confidence score, generates conflict report, and creates the verdicts record.",
  "instruction": { "$file": "instruction.md" },
  "toolsets": ["POD"],
  "visibility": "POD",
  "output_schema": {
    "type": "object",
    "properties": {
      "session_id": { "type": "string" },
      "verdict_id": { "type": "string" },
      "verdict": { "type": "string", "enum": ["APPROVED", "REJECTED", "ESCALATED", "REFRAMED"] },
      "confidence_score": { "type": "number", "minimum": 0.0, "maximum": 1.0 },
      "conflict_report": { "type": "object" },
      "consensus_met": { "type": "boolean" }
    },
    "required": ["session_id", "verdict_id", "verdict", "confidence_score", "conflict_report", "consensus_met"]
  },
  "permissions": {
    "grants": [
      { "resource_type": "function", "resource_name": "write_db_record", "permission_ids": ["function.read", "function.execute"] },
      { "resource_type": "function", "resource_name": "update_db_record", "permission_ids": ["function.read", "function.execute"] },
      { "resource_type": "datastore_table", "resource_name": "verdicts", "permission_ids": ["datastore.table.read", "datastore.record.read", "datastore.record.write"] },
      { "resource_type": "datastore_table", "resource_name": "debate_sessions", "permission_ids": ["datastore.table.read", "datastore.record.read", "datastore.record.write"] }
    ]
  }
}
